/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "localhost"],
    // Add the domains you want to use
  },
};

module.exports = nextConfig;
